[rules]
    [rules.test-file]
    source = ["file", "/home/bitflipp/Test"]
    regexp = ".*"
    action = ["ban", "12h"]

    [rules.test-systemd]
    source = ["systemd", "NetworkManager"]
    regexp = ".*"
    action = ["log"]
